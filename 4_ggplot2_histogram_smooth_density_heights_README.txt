Get the heights dataset.

Create a ggplot object called p1 to assign the heights data to a ggplot object and make the histogram.

Add binwidth to histogram. bins of size 1 inch. 

Create a smooth density plot.

Create separte smooth density plots for males and females.
Assign groups through the color.

Create separte smooth density plots for males and females.
Assign groups through the color.

Use alpha blending.
