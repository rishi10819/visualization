Associate a murders dataset with a plot object.
Define the x-axis and y-axis variables for aesthetic mappings.
Add label for each data point.
Add blue color to labels.

Use color to represent the different regions. 
So the states from the West will be one color, states from Northeast another, and so on.

Change the axes to log scales.

Add plot title.